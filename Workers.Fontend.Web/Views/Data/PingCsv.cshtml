@{
    Layout = null;
    Response.ContentType = "text/csv";
    Response.AddHeader("Content-Disposition", "attachment;filename=ping.csv");
}
@model List<Workers.Models.PingerModel>
date,url,httpStatus
@foreach(var ping in Model)
{
    @:@ping.Time.ToLocalTime(),@ping.Url,@ping.Status
}